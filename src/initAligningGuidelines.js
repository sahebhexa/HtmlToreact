function initAligningGuidelines(canvas) {
    // code to initialize or update the aligning guidelines on the canvas
    // for example:
    canvas.forEachObject(function(obj) {
      // update the guidelines for each object on the canvas
    });
  }